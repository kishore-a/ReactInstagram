import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    flexDirection:'row',
    
  },
  text: {
  top: -5,
  marginLeft:5,
  fontWeight:'bold',
  fontSize:15
  },
});
export default styles;
